const Switch = (props) => {
    return (
        <div className="switch">
            <input type="checkbox" />
            <span class="slider round" />
        </div>
    )
};

export default Switch;